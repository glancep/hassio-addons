# traefik/whoami is a static Go binary that listens on :80 and echoes every
# header it receives back in the response body. No entrypoint wrangling
# needed — this is purely a diagnostic tool, delete this add-on when done.
FROM traefik/whoami:latest
